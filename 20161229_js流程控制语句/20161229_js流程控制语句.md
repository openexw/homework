#### 输入三个整数x,y,z，请把这三个数由小到大输出
~~~
var arry=[54,787,89];
for(var i=0;i<=arry.length ;i++)
{   var a;
    if(arry[i]<arry[i+1]){a=arry[i];arry[i]=arry[i+1];arry[i+1]=a;}
    console .log(arry);
}
~~~
#### 判断101-200之间有多少个素数，并输出所有素数。
~~~
var i,j;
for(i=100;i<200;i++ )
{
    for (j=2;j<=(i/2);j++)
    { if (i%（j）/2==0) break;}//跳出本层循环

       if (i%j!=0)
       console.log("%d\t",i);
     }
       console.log("\n");
~~~

#### 输入两个正整数m和n，求其最大公约数和最小公倍数。（利用辗除法）

~~~
btn.onclick=function(){
var val=inpurt.value;
var arr=val.spllit(",");
var x=+arr[0];
var y=+arr[1];
for (var i=1;<=x*y;i++)
     {
        if(x%y==0&&y%i==0){break;console.log()}
     }
}

<!--for(var i=0 ;i<=b;i++){
    if((a%b)==0){console .log(b)}
    c=(a%b);
    a=b;
    b=c;*/
}-->
~~~
#### 有一分数序列：2/1，3/2，5/3，8/5，13/8，21/13...求出这个数列的前20项之和。
~~~
var a=1;
var b=2;
var c;var d=0;
for(var i=0;i<20;i++)
{    d+=(b/a);
    c=a;
    a=b;
    b=c+b;
}
console .log(d)
~~~
#### 给一个不多于5位的正整数，要求：一、求它是几位数，二、逆序打印出各位数字。
~~~
var a=1234159876;
var gh=a.toString ();
var b=gh.length;
var kl=[];
var jk=[];
for(var i=0,j=b;j>1,i<b;i++,j--)
{
  kl[i]=+gh[i];
  jk[b-1]=kl[i];
}
console.log(jk);
var b=kl.length ;
console .log(kl)
for(var i=0;i<b;i++)
{

    for(var j=i;j<b;j++){
       var gy;
      if( kl[i]<kl[j+1])
      {
          gy=kl[i];
          kl[i]=kl[j+1];
          kl[j+1]=gy;
      }
    }
}
console .log( kl);
~~~
#### 请输入星期几的第一个字母来判断一下是星期几，如果第一个字母一样，则继续判断第二个字母。 将一个数组逆序输出。
~~~
btn_week.onclick = function(){
        var val = input.value;
        var f = val[0];
        if(val.length >1){
            var s = val[1];
        }

        var res = "";
        switch (f){
            case "S":
                if(s == "u"){
                    res = "Sunday";
                }else if(s == "a"){
                    res = "Saturday";
                }else{
                    res = "请输入第二个字母"
                }
            break;
            case "T":
                    if(s == "u"){
                        res = "Tuesday";
                    }else if(s == "h"){
                        res = "Thursday";
                    }else{
                        res = "请输入第二个字母"
                    }
                break;
            case "M":
                    res = "Monday";
                break;
            case  "W":
                res = "Wednesday";
            case  "F":
                res = "Friday";
                break;
            default:
                res = "您搞错了";
        }

        h1.innerText = res;
    }


~~~
#### 将一个数组逆序输出。

~~~
var arr = [1,3,5];
       var tem = [];
       for(var i=arr.length; i>0; i--){
           tem.push(arr[i-1]);
       }
       console.log(tem);
~~~

